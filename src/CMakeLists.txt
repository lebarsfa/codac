# ==================================================================
#  Codac - cmake configuration file
# ==================================================================


# Compiling sources
# =================

  add_subdirectory(core)
  add_subdirectory(graphics)
##################  add_subdirectory(unsupported)

  if(WITH_CAPD)
    add_subdirectory(extensions/capd)
  endif()

  if(WITH_IBEX) # included by default for now
    add_subdirectory(extensions/ibex)
  endif()

  #if(WITH_PYTHON)
  #  add_subdirectory(sympy)
  #endif()
  

# Generating file for #include <codac>
# ====================================

  set(CODAC_MAIN_HEADER ${PROJECT_BINARY_DIR}/codac)
  file(WRITE ${CODAC_MAIN_HEADER} "/* This file is generated by CMake */\n\n")
  file(APPEND ${CODAC_MAIN_HEADER} "#pragma once\n\n")
  foreach(header_path ${CODAC_MAIN_SUBHEADERS})
    get_filename_component(header_name ${header_path} NAME)
    file(APPEND ${CODAC_MAIN_HEADER} "#include <${header_name}>\n")
  endforeach()

#if (CODAC_INSTALL) # To find how to set the default value of this option...
  install(FILES ${CODAC_MAIN_HEADER} DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
#endif()
